# No need to set EXPO_PUBLIC_API_BASE_URL — the app auto-detects your Mac's IP via Expo
# Only set this if you need to override (e.g. pointing to a deployed server):
# EXPO_PUBLIC_API_BASE_URL=https://your-production-server.com/app/api/v1
